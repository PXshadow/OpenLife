import openlife.data.map.MapInstance;
import openlife.data.object.player.PlayerInstance;
import openlife.data.object.player.PlayerMove;
import openlife.data.map.MapChange;
import haxe.Timer;
function main()
{
    var bot = new Bot(new openlife.client.Client()); //do not change this line, needs to be the first
    trace("started script");
    bot.event.playerUpdate = function(instances:Array<PlayerInstance>)
    {
        trace("player " + instances[0].p_id + " updated");
    }
}