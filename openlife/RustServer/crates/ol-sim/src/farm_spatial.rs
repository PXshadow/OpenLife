//! Re-export note for **AI-JOB-FARM-WIRE** / `farm_spatial`.
//!
//! Spatial helpers live in **`ol-ai-professions`** `farmer_profession` (via
//! `farm_spatial_inc.rs` include in that crate). Sim `farmer_profession` is a
//! thin `pub use` of the professions crate.
//!
//! Public API is re-exported from `ol_sim` (`fill_farm_counts_from_map`,
//! `try_decide_farm_from_rung`, …).
