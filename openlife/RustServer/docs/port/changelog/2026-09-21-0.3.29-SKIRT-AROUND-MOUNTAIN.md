# 0.3.29 — walk to a skirt the NPC can actually reach

Heartbeat: 2026-09-21T21:40:00Z

Live 0.3.28: Eve Hall and Eve Wilson ran `clothing_craft CraftItem(128)` toward rope 59 at (478, 82) and then stayed on `drop_held_walk` at (477, 48). That rope is south of a snow-grey band (biome 21, movement blocked). Chebyshev said it was inside 60. The walk's first step into the band was rejected, so the pickup never finished and `switchCloths` never ran.

Haxe `isBlocked` / `gotoObj` does not step onto that biome. Path steps now use the same rule as movement. Reed Skirt craft picks the closest rope and reed a walk can reach. A pair behind the band is skipped so the craft can use thread and reed on the near side, then DROP the skirt and SELF wears it.
