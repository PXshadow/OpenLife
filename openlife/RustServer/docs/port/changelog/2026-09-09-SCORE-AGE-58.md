# 2026-09-09 SCORE-AGE-58

**status:** **DONE**

Haxe `TimeHelper.updateAge` L808-839: once when age year is 58, GM
`Your life nears the end. You earned ${floor(yum_multiplier)} prestige!`
then if `DisplayScoreOn`, extra GMs for children/grandkids/followers/eating/wealth/parents/siblings
when that bucket is `> 5` (floored except wealth), amounts x `DisplayScoreFactor`.

- `ol-sim/score_age.rs` builds the Haxe texts from lineage prestige + OLN15 `prestige_from`
- `tick_vitals` fires once when `true_age` crosses 58 (AGE-10 `crossed_true_age_year`)
- `DisplayScoreFactor` from LiveSettings; `DisplayScoreOn` Haxe default true (not Live)
- AI / empty strings skipped (`sendGlobalMessage`)

Tests: `age58_score_gm_texts_once` / `score_age::tests::*`

`cargo check -p ol-server` Finished.

Residual: Haxe gates on `Std.int(player.age)` (display age); leftover uses true_age. DisplayScoreOn is SETTINGS-KNOB-TAIL. Next **NESTED-PERSIST-SER**.
