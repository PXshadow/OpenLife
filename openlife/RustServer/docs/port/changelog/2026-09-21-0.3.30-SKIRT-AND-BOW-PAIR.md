# 0.3.30 — use a ready skirt pair and a ready bow pair

Heartbeat: 2026-09-22

0.3.29 treated a rope as unreachable when a side-step search gave up, so Reed Skirt craft kept walking raw plants and `switchCloths` did not run. Haxe `gotoObj` walks while the next step is not `isBlocked` and gets closer. Plain snow (biome 4) stays walkable. Mountain snow-grey (biome 21) still blocks, which is Haxe `SNOWINGREY`.

`craftItem(128)` again DROP-picks rope 59 and USEs reed 124 when both are inside 60 and a step can move closer. `getWeapon` / `craftItem(152)` does the same for rope 59 and yew shaft 131 (Yew Bow 151). Holding 151 or 148 combines with the other half and does not drop the bow to fetch rope. After the skirt USE, `isDropingItem` DROP picks up 128 and `switchCloths` `self()` wears it.
