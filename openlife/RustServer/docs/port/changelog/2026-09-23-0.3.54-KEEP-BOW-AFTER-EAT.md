# 0.3.54 — do not drop a yew bow as a food peel

Haxe `isEating` drops a leftover only while that peel is still in hand. Rust kept `eat_pending_peel` after the hand was empty, then dropped the next pickup. Live: eat 768, later DROP-pick yew bow 151, next think `eat_peel_drop` put the bow back on the ground. An empty hand clears the flag. A peel still held is unchanged.
