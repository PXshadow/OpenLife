# AI-LLM-FAN — live `fan_out_ai_speech_llm`

## Haxe

- `AiBase.sayHelper` ~L4971–5001 after scripted cmds
- Gate: human speaker, `IsLLMActivated`, age>3, no leading `!`/`?`, cooldown 4s
- Immediate: `doEmote(oreally)`, ally `Goto(self)` + `setWaitingTimeMin(6)`, `say("...")`
- Then `AiHandler.respondToPlayerAsync` (skip AIs that already handled a scripted cmd)

## Rust

| Symbol | Role |
|--------|------|
| `fan_out_ai_speech_llm` / `_ex` | live SAY fan-out after `fan_out_ai_say_scripted` |
| `tick_llm_speech_wire` | import results, apply sticky/chunks/PE, export jobs |
| `SimState.llm_speech_jobs/results/io` | queues + HTTP share |
| `take_llm_speech_jobs` / `push_llm_speech_result` | drain API |

Skips `scripted_handled` conn_ids. `llm_activated` from env (`AI_API_KEY`); tests use `_ex`.

## Tests

```
cargo test -p ol-sim --lib -- fan_out_ai_speech_llm fan_out_ai_say_scripted tick_llm_speech_wire say_you_are -- --test-threads=1
```

## Residual

1. lastNames.txt 2-letter map + gender-split first-name files
2. Full live `RelationshipView` (exile/leader/prestige) in prompt
