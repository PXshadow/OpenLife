# 2026-09-02 FORCE

**status:** **DONE**

Haxe `MoveHelper.receivedForce`: client `FORCE x y` clears `waitForForce` only when coords match the body.

MOVE while `still_waiting_for_force` (2s) is silent (`MoveReject::WaitForForce`, no second CancleMovement). Timeout clears wait and accepts MOVE.

Residual: CancleMovement does not yet **set** `wait_for_force` on humans (flag is persist/test-driven until **FORCE-ARM**).

Tests: `force_clears_wait_when_coords_match` / `move_ignored_while_waiting_for_force`.

`cargo check -p ol-server --offline` ok.
