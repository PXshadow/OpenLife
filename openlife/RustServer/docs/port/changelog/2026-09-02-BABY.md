# 2026-09-02 BABY live pickup

**status:** **DONE**

Raw `BABY x y [id]` → `apply_baby` → `apply_do_baby_hold` (shared with SAY `HOLD`). Haxe `GPI.doBaby` / `doBabyHelper`: `GetPlayerAt` (exact id, else closest ≤ 1.5 wrap) → `PickupBabyMaxDistance` 1.9 → empty hands → age gates → pickup restore/exhaustion/follow. Fail always PU+FRAME. HOLD fail also PU+FRAME.

Test: `baby_tag_picks_up_grounded_child`.

Residual: DROP while holding a player still no-ops (`GPI.drop` → `dropPlayer`); SWAP holding-player currently JUMP-wiggle not `dropPlayer(x,y)`. Nested `target.dropPlayer` on pickup still refused.
