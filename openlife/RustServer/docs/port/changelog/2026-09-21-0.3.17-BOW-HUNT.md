# 0.3.17 — GetOrCraftItem(152) USEs ready 59+131; dummyParent on craft graph

Heartbeat: 2026-09-21T17:10:00Z

Haxe `getWeapon(true)` → `GetOrCraftItem(152)` → on miss `craftItem(152)` which USEs the best ready pair (`craftItemHelper`). Live census had Rope 59 and Yew Shaft 131 (14) but Bow 151/152 stayed 0: `seek_ingredient_for` saw a ready 59+131 path then fell through to missing Arrow 148.

`seek_ingredient_for` now returns None when the first path step is ready so `CraftItem(152)` fires 59+131 → Yew Bow. Graph seed uses `dummyParent` so `@ Yew Bow` 494 matches ground 151; then 148+151 → Bow and Arrow. Wolf shot remains `use()` on 418 (`152_418`).
