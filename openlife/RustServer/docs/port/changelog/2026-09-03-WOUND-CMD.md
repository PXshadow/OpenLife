# 2026-09-03 WOUND-CMD

**status:** **DONE**

Haxe `TransitionHelper.doCommandHelper` `heldObject.isWound()` after neverDrop:

- Visible wound: refuse USE/DROP/SWAP/REMV; countdown say `N seconds...` when ttc remaining > 0.
- Hidden wound alias: `clear_held` then continue (REMV can take; empty-hand USE applies). `hidden_wound` stays.

Tests: `drop_visible_wound_refuses` / `swap_visible_wound_refuses` / `use_visible_wound_refuses` / `drop_visible_wound_countdown_say` / `use_hidden_wound_clears_and_continues` / `remv_hidden_wound_clears_and_takes`.

`cargo check -p ol-server --offline` ok.
