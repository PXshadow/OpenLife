# 2026-09-05 FIRE-CRAFT-R30

**status:** **DONE**

Haxe `isHandlingFire` wraps `itemToCraft.maxSearchRadius=30` around nested `makeFireFood(3)` on hot-coals firePlace (`craft only close`). Restore only when makeFireFood returns false (kindling GetOrCraft). Near-player coals `makeFireFood(2)` is not wrapped.

- `FIRE_CRAFT_HOT_COALS_SEARCH_RADIUS` + `FireKeeperProfessionRuntime.craft_search_radius_override`
- Set on MakeFireFood(3); clear on empty expand (Haxe restore `tmpSearchRadius`)
- Keep 30 when makeFireFood acts (CraftItem / ShortCraft)
- Live player scan + npc ladder: `apply_fire_craft_search_radius_override` copies onto `itemToCraft.maxSearchRadius` then takes

Tests: `hot_coals_make_fire_food_3_sets_craft_r30` / `hot_coals_make_fire_food_3_success_keeps_r30` / `handling_fire_scan_hot_coals_make_fire_food_3_sets_r30` / `apply_profession_scan_tick_hot_coals_craft_copies_r30`

`cargo check -p ol-server --offline` ok.
