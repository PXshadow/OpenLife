# 0.3.22 — isPickingupCloths stays local; GetOrCraft clothing caps at r=60 then recrafts (Haxe L8723 / L6198)

Heartbeat: 2026-09-21T18:35:00Z

Live 0.3.21: `pickup_cloth`=16 but worn=0, `clothing_craft`=0. Reed Skirt 128 stayed at 228,275 (Chebyshev 104 from the nearest adult). Unbounded world-search in `isPickingupCloths` (L652) always found that orphan and walked toward it, so `craftHighPriorityClothing` (L684) never recrafted at the new home.

Haxe `isPickingupCloths` L8723 only `GetCloseClothings` r=8. Haxe `GetOrCraftItem` L6150–6198 searches `maxSearchDistance` (clothing pass 60) then `craftItem`. Rust now: pickup uses r=8 plus home r=60 only; clothing `CraftItem` DropAts an existing 128 only inside r=60, else crafts at home.
