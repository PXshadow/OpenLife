# 0.3.21 — pick up existing Reed Skirt even when far from home (Haxe GetOrCraftItem L6196)

Heartbeat: 2026-09-21T18:15:00Z

Live 0.3.20: Skirt 128=1 unworn at 228,275 (Chebyshev 205 from new Eve home 433,245). `pickup_cloth`=0. GetCloseClothings r=8 and home r=60 never saw it; GetOrCraft maxSearchDistance=40 recrafted instead. Rope 59 was not at the new home, so 59+124 never ran.

Haxe `GetOrCraftItem` L6196: if the object exists, `dropTarget = obj`. `isPickingupCloths` now world-searches a switchable ground cloth after the r=8/home r=60 miss; clothing `CraftItem` DropAts the closest existing product before expanding the craft graph.
