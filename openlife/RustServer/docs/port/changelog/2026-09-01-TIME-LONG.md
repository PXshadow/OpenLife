# TIME-LONG / nested multi-use decay + live tick wire

## Chunk
- **matrix_id:** `TIME-LONG`
- **status:** **DONE**
- **Haxe:** `TimeHelper.DoTimeStuff` L153–157 `DoWorldMapTimeStuff` then `DoWorldLongTermTimeStuff`; TODO L1479 multi-use / containers
- **Rust:** `tick_world_after_players` → `apply_live_world_time_bands`; `try_decay_object` uses−−; `try_decay_contained` nested slots + contained ids

## Implemented
1. `SimState.long_term: LongTermState`
2. Live tick: map-time Y-band then long-term Y-band, MX fan-out (moving water-drift uses `format_map_change_moving`)
3. Multi-use long-term: `num_uses > 1` && `uses > 1` → decrement uses, keep id
4. Nested container long-term: decay contained ids + recursive `NestedHelper` slots/uses

## Residual
- Haxe `RespawnObjects` between the two bands (long-term already has spring original-tile respawn)
- Exact `SpringRegrowChance` vs years-scaled long-term offspring (next: **PLANT-OFFSPRING**)

## Verify
```powershell
cargo test -p ol-sim --lib -- long_term_multiuse long_term_nested_contained tick_world_after_players_advances -- --test-threads=1
cargo check -p ol-server --offline
```
