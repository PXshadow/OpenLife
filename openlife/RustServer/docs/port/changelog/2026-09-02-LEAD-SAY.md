# 2026-09-02 LEAD-SAY

**status:** **DONE**

Haxe `doCommands` `!L`/`!LEADER`/`?L`/`?LEADER`/`!DL`/`?DL`: personal leader power/map (not ranking).

SAY intercepts `parse_leader_personal_command` **before** `?LEAD*` leaderboard. Bare `LEADER` still ranking. `?LEADERBOARD` unchanged.

`!L` → power + map pin; `?L` → power only; `!DL` with no follow → `No leader!`.

Tests: `say_bang_l_sends_power_and_map_pin` / `say_query_l_power_only_and_dl_no_leader`.

`cargo check -p ol-server --offline` ok.
