# 0.3.8 — doTimeStuffHelper L635–655 order

Heartbeat: 2026-09-18T16:00:00Z

After `isMoving()` return, Haxe runs:

`isHandlingFire` → `doKnifeStuff` → `shortCraft(0,400)` → `isPickingupCloths` → `handleTemperature` → `isHunting` (age>14) → `makeSharpieFood(5)` → clothing.

Rust had fire then clothes then sharpie, and hunting only later in the profession ladder. Mid path now matches L635–656.
