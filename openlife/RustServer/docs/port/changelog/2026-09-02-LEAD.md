# 2026-09-02 LEAD

**status:** **DONE**

Haxe `Connection.sendLeader` / `sendLeaderHelper`: client `LEAD` → PS map pin of top leader (`LEADER *leader p_id *map x y`) + FM. No power say (that's `!L`).

`ClientTag::Lead` so protocol parses `LEAD`. Live `apply_leader_query(..., direct=false, with_map=true, with_power=false)`.

Test: `lead_tag_sends_leader_map_pin`.

`cargo check -p ol-server --offline` ok.
