# 0.3.12 — isDropingItem picks up skirt after 59+124→128

Heartbeat: 2026-09-21T14:40:00Z

Live 0.3.11: `clothing_craft` Reed Skirt 128 rose, clothing slots stayed empty, `pickup_cloth`/`switch_cloths` stayed 0. Hunt `wait_weapon` stayed 0 (0.3.11 moving-Wait fix held).

Haxe `isStillExpectedItem` (AiHelper L589) compares `dropTarget.parentId` to the live tile. Staging pickup with expected 124 kept the sticky (`sticky_move_still_valid` clothing exception) but `is_dropping_item_head` still `TargetGone` on 124≠128, so the DROP never ran. Pickup-cloth stickies now wait for sim apply instead of dropping onto Reed Bundle 124, then use live parent 128 for `isDropingItem` DROP → `switchCloths` `self()`.
