# 0.3.5 — AI food, loaded ServerAi, Haxe isMoving, tool crafts

Heartbeat: 2026-09-17T12:00:00Z

Haxe parity for live NPC think (no invented MOVE timeout):

- `ReadPlayers` ServerAi: count loaded AIs before Eve LOGIN; full think on `ai_controlled` bodies.
- `setParentFoods` / `foodFromTarget`: empty-hand harvest (bush 30, banana plant 2142, onion 805).
- `isEating` → `self()` (`SELF 0 0 -1`), not ground USE.
- `waitForForce` is not restored from PU `forced`.
- `isConsideringMakingFood` does not SearchBestFood when not hungry; leftover foodTarget does not block `makeSharpieFood(5)`.
- `isHandlingFire()` before `makeSharpieFood(5)` (Haxe L634 then L656).
- MOVE: `isMoving()` = `newMoves != null` (`move_path`); send only if dest changed or not moving. Same sim tick as `move()` counts as moving.
