# 0.3.9 — pickup/switch clothes; wolf home search uses AnimalWorld

Heartbeat: 2026-09-18T18:00:00Z

Haxe `isPickingupCloths` stages dropTarget/useTarget; `isDropingItem` / `isUsingItem` walk and pick up; `switchCloths` `self()` wears held clothing. Rust now walks/DROP/USE the clothing tile in the same think when found.

Haxe `killAnimal` L5884 `GetClosestObjectToPosition(home, 418, 20)`. Live wolves are AnimalWorld movers — include them in that home square so `getWeapon(true)` → GetOrCraftItem(152) can run.
