# 2026-09-02 FORCE-ARM

**status:** **DONE**

Haxe `MoveHelper.CancleMovement`: humans set `waitForForce` + `timeLastForce`.

Live: MOVE reject `JumpTooFar` / `BlockedStart` / `EmptyPath` / `JumpRateLimited` → force PU then `arm_human_wait_for_force`. TooYoung/silent do not arm. `cancel_movement` also arms. AIs skipped (`isHuman`).

Tests: `cancle_movement_wait_cleared_by_force` / `move_path_large_jump_rejected`.

`cargo check -p ol-server --offline` ok.
