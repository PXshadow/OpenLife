# 0.3.15 — held stalk/thread uses same-id secondObject (Haxe L6997 / L7935)

Heartbeat: 2026-09-21T16:20:00Z

Live 0.3.14: census Stalk 57 climbed (10→18), Thread 58 stuck at 4, Rope 59=0, Skirt 128=0. AIs held 57 while two threads already existed, so BFS `find_path_to_product` returned 58+58 first and never 57+57.

Haxe `craftItemHelper` L6997: if held == transActor, USE secondObject. Same-id `DoTransitionSearch` L7935 uses `actor.secondObject`. Held is `closestObject` (distance 0), so 57+57 on a nearby stalk beats far 58+58.

Rust `held_ready_step` now prefers a ready pair that uses held and produces an ingredient of the wanted product, before the BFS path.
