# 0.3.29 — a rejected skirt walk is a failed goto

Heartbeat: 2026-09-21T23:55:00Z

Live 0.3.28: `switch_cloths` and `pickup_cloth` still never logged, and nobody held 128. One NPC walked `CraftItem(128)` toward 462,471, then sat on 466,467 resending `drop_held_walk` until food went negative. The sim was not setting `isMoving` (empty/blocked path), and the next think sent another MOVE from the same tile, which cancels the path. `isDropingItem` never reached the DROP at L8456, so `switchCloths` L8709 never saw the skirt.

If a MOVE toward the same goal was already sent from this tile and the body is still there and not moving, treat `gotoObj` as failed: clear `dropTarget` and mark the tile not reachable for 90s, which is Haxe L8442–8446.
