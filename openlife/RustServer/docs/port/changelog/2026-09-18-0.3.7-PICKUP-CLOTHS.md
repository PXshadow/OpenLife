# 0.3.7 — isPickingupCloths before makeSharpieFood

Heartbeat: 2026-09-18T14:00:00Z

Haxe doTimeStuffHelper L652 `isPickingupCloths()` then L656 `makeSharpieFood(5)`. Rust ran sharpie first, so plant-rich maps never reached clothing. Pickup clothes from the ground now runs on the stopped-jobs path before digging carrots/burdock.

Clothing craft activity is always tagged `clothing_craft` (including walks).
