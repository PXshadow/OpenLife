# 0.3.16 — pick up existing Reed Skirt (Haxe GetOrCraftItem L6196 / GetCloseClothings + home)

Heartbeat: 2026-09-21T16:30:00Z

Live 0.3.15: Thread 58 4→7 and census Skirt 128=1, but `pickup_cloth`=0 and worn=0. `clothing_craft` kept recrafting the 57/58 chain while the skirt sat at home; `isPickingupCloths` only scanned r=8 around the player so hunters never saw it.

Haxe `GetOrCraftItem` L6196: if the object exists, `dropTarget = obj` (DROP pickup). Haxe `addAllObjectsForCrafting` L7240 always scans home; `CountCloseObjects` clothing stock uses r=60.

Rust now: clothing `CraftItem` picks up an existing 128/200/… before expanding the craft graph; `DropAt` on a clothing tile is labeled `pickup_cloth`; `isPickingupCloths` also scans home r=60.
