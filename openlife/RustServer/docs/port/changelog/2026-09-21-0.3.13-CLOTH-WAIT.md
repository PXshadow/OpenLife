# 0.3.13 — isDropingItem waits for 128 without triedDropCount abort

Heartbeat: 2026-09-21T15:00:00Z

Live 0.3.12: `clothing_craft` Reed Skirt 128 rose (10), clothing slots stayed empty, `pickup_cloth`/`switch_cloths` stayed 0, no bow 152. `triedDropCount` from `craft_queue_drop` / `droping_drop_held` is never reset (Haxe resets when `dropTarget == null`). Wait-for-128 reused that counter and aborted after 3, so the DROP never ran.

Haxe `isDropingItem` L8361 `triedDropCount` only shortens `dropDistance` (10→0). Pickup wait for async 59+124→128 now uses a dedicated counter (max 20 thinks), resets when staging `pickup_cloth`, and only waits while the live tile is still the expected Reed Bundle. Empty tile is TargetGone. `isDropingItem` gets the real `isMoving()` flag. `pickup_cloth` / `switch_cloths` log as Craft so `/api/npc/stats` sees them. `getWeapon` SeekOrCraft keeps `kill_animal_seek_weapon` as the activity prefix (GetOrCraftItem L6152).
