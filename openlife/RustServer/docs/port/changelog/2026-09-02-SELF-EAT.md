# 2026-09-02 SELF-EAT

**status:** **DONE**

## Closed

- Haxe `GlobalPlayerInstance.doSelf`: `clothingSlot < 0` → `doEating`.
- Live SELF order: drink → `try_eat_held` when slot `< 0` → clothing (`apply_self_clothing_after_drink`).
- Eat FX + eat PU + FM via `send_held_eat_result` (same as USE eat).
- Slot `>= 0` does not eat. Water bowl still drinks before eat.

## Tests

- `self_minus_one_eats_held_food` — eat on `-1`, no eat on slot 5, drink before eat
- `clothing_transitions::*` still 58 ok

`cargo check -p ol-server --offline` ok.
