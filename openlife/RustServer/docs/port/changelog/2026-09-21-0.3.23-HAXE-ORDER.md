# 0.3.23 — think order is Haxe doTimeStuffHelper, not parallel “competition”

Heartbeat: 2026-09-21T18:45:00Z

Haxe `doTimeStuffHelper`: killAnimal L593 returns → helper ends. isPickingupCloths L652 is GetCloseClothings r=8 around the player. craftHighPriorityClothing L684 is craftItem with searchCurrentPosition=false, maxSearchRadius=60 from home.

Rust had GetOrCraftItem isMoving Wait fall through so later bands ran, isPickingupCloths merged home r=60, and a second pickup in the job-rung loop. Those are gone. Existing 128 for clothing craft is looked up from home r=60.
