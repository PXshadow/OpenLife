# 2026-09-02 FLIP

**status:** **DONE**

Haxe `Connection.flip`: client `FLIP x y` fans `FL p_id true|false` to nearby (`isClose` PU range). `face_left = x < player.x` after birth-relative → world resolve.

`ClientTag::Flip` so protocol parses `FLIP`. Live `apply_player_flip`. No FRAME (Haxe `send(FLIP)` only).

Tests: `flip_tag_sends_fl_face_left` / `parse_flip_as_raw` / `flip_server_tag_and_format`.

`cargo check -p ol-server --offline` ok.
