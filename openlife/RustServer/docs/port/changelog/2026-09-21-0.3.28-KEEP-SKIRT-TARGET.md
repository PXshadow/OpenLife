# 0.3.28 — keep the skirt drop target, then wear it

Heartbeat: 2026-09-21T23:40:00Z

Live 0.3.27: `switch_cloths` never logged, no player held 128, every clothing slot stayed 0. `droping_drop_held` ran 1000+ times. `isDropingItem` (held object, skirt quad > 25) calls `dropHeldObject(10)`, and Haxe only clears `dropTarget` when `maxDistanceToHome < 1` (L5281). Rust cleared the sticky anyway, so the later DROP (L8456) never picked up Reed Skirt 128.

`isFeedingChild` also returns before `switchCloths` (L557 then L8709). Once 128 is in hand, a hungry child makes the NPC walk off or `dropHeldObject(0)` the skirt. If the held object is clothing that `shouldSwitchCloth`, SELF wears it before that baby drop. Rope 59 is not clothing, so the 0.3.27 reed USE still runs first.
