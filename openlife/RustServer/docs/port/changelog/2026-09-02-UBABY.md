# 2026-09-02 UBABY

**status:** **DONE**

## Closed

- Haxe `GlobalPlayerInstance.doOnOther` / `doOnOtherHelper` live on Raw `UBABY x y i [id]`.
- Order: drugs-only-if-ill → `isCloseToPlayer` (range 1) → `doEating` when slot `< 0` (`try_do_eating`) → `doSwitchCloths` (age gate) → wounded `GetTrans(held, target.held)`.
- `GetPlayerAt`: exact `p_id` if `id > 0`, else closest within 1.5 (wrap). Fail says: need to drop held / no one found / too far away.

## Tests

- `ubaby_feed_cloth_heal_live` — feed, cloth hat, too-far refuse, bandage-heal wound
- `try_eat_held_blocks_yellow_fever` still ok

`cargo check -p ol-server --offline` ok.
