# 0.3.10 — AiBase think order; wear after craft; bow GetOrCraft r=60

Heartbeat: 2026-09-21T20:00:00Z

NPC think in `npc_ai.rs` follows Haxe `doTimeStuffHelper` L416–L873:

- Before L599: mid-path skip → countSeeds → escape → drop/close-use → waiting → hungry infant (walk 5 then 3) → child-with-mother (walk, nested handleTemperature, nice-baby) → wounded → death → isEating → feed child → switchCloths → consider-food → far use → container → pickup food → temp → attack (GetClosePlayerTarget) → stay-close → killAnimal → feed player in need → follow → moving skip.
- After L599: foundFamily/allyUp → CriticalCraft (last-SMITH, kiln, bloom, omelette, first carrot, berry/bean held) → fire → knife → second carrot → clothes pickup → temperature → lambs → hunting → sharpie → graves → bucket → skewer 139+2832 → craft queue → high clothing, medium if age>10 → assigned → doCriticalStuff → low → home → drop held → idle.

Haxe `craftClothIfNeeded` `craftItem(128)` puts Reed Skirt on the ground (`59_124` → newActor 0, newTarget 128). Wear is `isPickingupCloths` + `isDropingItem` + `switchCloths` `self()`. After a USE whose newTarget is clothing, Rust stages that sticky so the next think picks up before food.

Haxe `getWeapon(true)` → `GetOrCraftItem(152)` with `itemToCraft.maxSearchRadius = 60`. Seek-or-craft now scans 60 and falls through to `CraftItem(152)` on a shallow miss. Wolf shot remains `use()` on 418.

Removed extra tails: `evaluate_nearby_crafts`, peel drop-at-feet before home, hunter HIT, 6-dir explore.
