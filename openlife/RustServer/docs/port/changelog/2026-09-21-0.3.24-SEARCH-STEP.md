# 0.3.24 — clothing craft search steps 30 then 60 (Haxe)

Heartbeat: 2026-09-21T20:10:00Z

Haxe `searchBestObjectForCrafting` adds `AiMaxSearchIncrement` (30) until `maxSearchRadius` and returns on the first radius that finds a transition. Before `craftHighPriorityClothing`, L672 sets `searchCurrentPosition = false` and `maxSearchRadius = 60`.

Rust clothing search had `ai_max_search_increment = 60`, so it skipped the 30 pass. It is 30 again. Pickup of clothes on the ground stays `GetCloseClothings` r=8. `craftItem(128)` is the wider home search, capped at 60.
