# 0.3.14 — Rope 59 needs two Thread 58 (same-id count)

Heartbeat: 2026-09-21T15:30:00Z

Live 0.3.13: tick advanced, `clothing_craft` Reed Skirt 128 rose, census 128=0 / Rope 59=0 / Thread 58=2, adults naked, `pickup_cloth`=0. `kill_animal_seek_weapon` rose with Bow 152=0 and Wolf 418 plenty.

Haxe `DoTransitionSearch` L7935 uses `secondObject` when actor==target (`58_58` → Rope 59, `57_57` → Thread 58). Rust `find_path_to_product` treated a HashSet `{58}` as both sides ready, so `craftItem(128)` never harvested Milkweed `0+50` and never combined two threads. Same bottleneck blocks Yew Bow `59+131` → 152.

Planner now requires count≥2 for same-id pairs (held+ground). Clothing / GetOrCraft scans also merge home tiles (Haxe `addAllObjectsForCrafting` L7240).
