# 0.3.6 — killAnimal shortCraft knife, clothing race, reed skirt fallback

Heartbeat: 2026-09-18T12:00:00Z

Haxe `killAnimal` L5905–5931: rattlesnake `shortCraft(560,764,10)` GetOrCrafts knife 560; on miss continue bow hunt `getWeapon(true)` → GetOrCraftItem(152). Rust no longer returns log-only `kill_snake_seek_knife`.

Haxe `craftHighPriorityClothing` L4492–4507: `getColor()` from person race; if loincloth 200 fails, Reed Skirt 128 same tick.

Profession-ladder `person_color` uses the same race (not hardcoded Brown/0).
