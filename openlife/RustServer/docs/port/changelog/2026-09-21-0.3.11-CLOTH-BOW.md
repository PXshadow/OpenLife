# 0.3.11 — pickup skirt after craft USE; hunt Wait while moving does not skip clothes

Heartbeat: 2026-09-21T21:30:00Z

Live 0.3.10: Reed Skirt CraftItem logs rose, clothing slots stayed empty, `kill_animal_wait_weapon` rose with no GetOrCraft 152.

Haxe `dropTarget` is the USE tile (59+124 → 128 on the same helper). Staging pickup with expected parent 128 cleared the sticky when the next think ran before sim applied the USE (tile still 124). Pickup sticky now expects the current tile; after apply, clothing on that tile stays valid.

Haxe `GetOrCraftItem` L6152 `isMoving()` returns true (no new goto). That Wait was consuming the think before `isPickingupCloths` L652 whenever AnimalWorld wolves kept `animalTarget` set. While already moving, Wait no longer consumes; after stop, SeekOrCraft(152) still runs.
