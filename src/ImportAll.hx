import auto.Automation;
import client.Client;
import client.ClientTag;
import client.Router;
import console.Command;
import console.Console;
import data.animation.AnimationChannel;
import data.animation.AnimationData;
import data.animation.AnimationParameter;
import data.animation.AnimationPlayer;
import data.animation.AnimationRecord;
import data.animation.AnimationType;
import data.animation.emote.EmoteData;
import data.animation.emote.Emotion;
import data.animation.SoundParameter;
import data.ArrayDataArrayInt;
import data.ArrayDataInt;
import data.ArrayDataTile;
import data.FractalNoise;
import data.GameData;
import data.LineReader;
import data.map.MapChange;
import data.map.MapData;
import data.map.MapInstance;
import data.map.SceneCell;
import data.map.SceneData;
import data.object.ObjectCode;
import data.object.ObjectData;
import data.object.ObjectKey;
import data.object.ObjectType;
import data.object.player.PlayerInstance;
import data.object.player.PlayerMove;
import data.object.SpriteData;
import data.object.ToolSetRecord;
import data.Point;
import data.Pos;
import data.sound.AiffData;
import data.sound.SoundData;
import data.sound.SoundPlayer;
import data.sound.SoundType;
import data.transition.Transition;
import data.transition.TransitionData;
import debug.Grid;
import debug.Profiler;
import game.Game;
import game.GameHeader;
import game.Ground;
import game.GroundOverlay;
import game.Objects;
import game.Player;
import game.Program;
import game.TileDisplay;
import game.Ui;
import game.Weather;
import Main;
import resources.ObjectBake;
import resources.ObjectSkim;
import server.Server;
import server.ServerTag;
import settings.Settings;
import Static;
import ui.Button;
import ui.InputText;
import ui.Text;
import UnitTest;
#if nativeGen @:nativeGen #end
class ImportAll
{
    public static function main() {
        trace("OpenLife imported");
    }
}